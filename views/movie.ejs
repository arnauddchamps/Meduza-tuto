<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>
        <%= movie.title %> | Node.js Blog</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm"
        crossorigin="anonymous">
    <style>
        body {
            padding-top: 20px;
            padding-bottom: 20px;
        }
    </style>
</head>

<body>
    <div class="container">
        <%- include('partials/navbar') %>
            <div>
                <h2>
                    <%= movie.title %>
                        <hr/>
                        <%= movie.test %>
                </h2>
                <p>by
                    <a href="#">
                        <%= movie.genre %>
                    </a>
                </p>

                <p>
                    <%= movie.synopsis %>
                </p>
                <!-- Button trigger modal UPDATE NAME-->
                <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#updateModal">
                    Modify
                    <%= movie.title %>
                </button>

                <!-- Modal  UPDATE NAME -->
                <div class="modal fade" id="updateModal" tabindex="-1" role="dialog" aria-labelledby="updateModal" aria-hidden="true">
                    <div class="modal-dialog" role="document">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h5 class="modal-title" id="updateModal">
                                    Modifyd
                                </h5>
                                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                    <span aria-hidden="true">&times;</span>
                                </button>
                            </div>
                            <div class="modal-body">
                                <form action="/movies/<%=movie._id %>/update" method="post">
                                    <div class="form-group">
                                        <label for="inputNewName">Name</label>
                                        <input type="text" class="form-control" id="inputNewName" name="title" aria-describedby="updateModal" placeholder="remplace it"
                                            value="<%=movie.title
                                            %>">
                                        <small id="inputNewName" class="form-text text-muted">Just for testing</small>
                                    </div>
                                    <button type="submit" value="OK" class="btn btn-primary">Modify</button>
                                </form>
                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                            </div>
                        </div>
                    </div>
                </div>
                <!-- Button trigger modal DELETE -->
                <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#deleteModal">
                    Delete </button>

                <!-- Modal  DELETE -->
                <div class="modal fade" id="deleteModal" tabindex="-1" role="dialog" aria-labelledby="deleteModal" aria-hidden="true">
                    <div class="modal-dialog" role="document">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h5 class="modal-title" id="deleteModal">Are you sure ?</h5>
                                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                    <span aria-hidden="true">&times;</span>
                                </button>
                            </div>
                            <div class="modal-body">
                                <p>You will delete
                                    <%= movie.title %>
                                </p>
                                <a href="/movies/<%= movie._id %>/delete">Delete</a>
                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                            </div>
                        </div>
                    </div>
                    <hr>
                </div>
                <%- include('partials/footer') %>
            </div>
</body>

</html>